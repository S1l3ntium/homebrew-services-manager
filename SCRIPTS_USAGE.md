# Build & Publish Scripts

## Обзор

Проект включает два полезных скрипта для сборки и публикации приложения:

- `build-app-bundle.sh` - Создает macOS .app bundle
- `PUBLISH.sh` - Автоматизирует весь процесс релиза

**Примечание:** Эти скрипты **НЕ отслеживаются** в git (добавлены в `.gitignore`), так как они содержат локальную конфигурацию.

---

## build-app-bundle.sh

Создает полноценный macOS .app bundle для распределения.

### Использование

```bash
./build-app-bundle.sh [version]
```

### Примеры

```bash
# Собрать v1.0
./build-app-bundle.sh 1.0

# Собрать с версией по умолчанию (1.0)
./build-app-bundle.sh
```

### Что делает скрипт

1. ✓ Собирает релиз бинарь (`swift build -c release`)
2. ✓ Создает структуру `.app` bundle
3. ✓ Копирует исполняемый файл в правильную папку
4. ✓ Создает `Info.plist` с метаданными
5. ✓ Подписывает приложение (ad-hoc)
6. ✓ Проверяет целостность bundle

### Результат

```
.build/release/HomebrewServicesManager.app/
├── Contents/
│   ├── Info.plist
│   ├── MacOS/
│   │   └── HomebrewServicesManager (исполняемый файл)
│   ├── Resources/
│   └── _CodeSignature/
```

---

## PUBLISH.sh

Автоматизирует полный процесс публикации релиза.

### Использование

```bash
./PUBLISH.sh [version]
```

### Примеры

```bash
# Опубликовать v1.0
./PUBLISH.sh 1.0

# Опубликовать с версией по умолчанию
./PUBLISH.sh
```

### Конфигурация

Отредактируйте строку 13 в `PUBLISH.sh`:

```bash
GITHUB_USERNAME="S1l3ntium"  # ← Измените на ваше имя пользователя
```

### Что делает скрипт

1. ✓ Вызывает `build-app-bundle.sh`
2. ✓ Создает zip-архив
3. ✓ Вычисляет SHA256 хеш
4. ✓ Создает git tag
5. ✓ Выводит инструкции по созданию релиза

### Результат

Скрипт выводит:
- Версию релиза
- Размер архива
- SHA256 хеш
- Инструкции по созданию GitHub Release
- Инструкции по обновлению Cask формулы

### Пример вывода

```
✅ Release preparation complete!

📋 Release Information:
   Version: 1.0
   Archive: HomebrewServicesManager.app.zip
   SHA256: a2a092558dcbdc37176b526ef7cdbb1aacbe2770281f1f0b8829b4a4e095dcc9
   Size: 166K

📝 Next steps:
1. Create GitHub Release...
2. Update Cask formula...
3. Users can install with...
```

---

## Полный процесс публикации релиза

### Шаг 1: Подготовка

```bash
# Убедитесь, что в PUBLISH.sh указано ваше имя пользователя GitHub
nano PUBLISH.sh  # Отредактируйте строку 13

# Коммитьте все изменения кода
git add .
git commit -m "Prepare for v1.0 release"
git push
```

### Шаг 2: Создание релиза

```bash
# Запустите скрипт публикации
./PUBLISH.sh 1.0
```

Скрипт выведет SHA256 и инструкции.

### Шаг 3: Создание GitHub Release

```bash
gh release create v1.0 \
  --title "v1.0 - Release" \
  --notes "Homebrew Services Manager v1.0" \
  .build/release/HomebrewServicesManager.app.zip
```

Или используйте веб-интерфейс: https://github.com/S1l3ntium/homebrew-services-manager/releases/new

### Шаг 4: Обновление Cask формулы

В вашем tap репозитории (`homebrew-tap`):

```bash
cd ../homebrew-tap

# Отредактируйте Casks/homebrew-services-manager.rb
nano Casks/homebrew-services-manager.rb
```

Обновите:
```ruby
version '1.0'
sha256 'a2a092558dcbdc37176b526ef7cdbb1aacbe2770281f1f0b8829b4a4e095dcc9'
```

```bash
git add Casks/homebrew-services-manager.rb
git commit -m "Update to v1.0"
git push
```

### Шаг 5: Готово!

Пользователи могут установить:

```bash
brew tap S1l3ntium/tap
brew install --cask homebrew-services-manager
```

---

## Зависимости

Скрипты требуют:
- `bash` (есть на macOS)
- `swift` (Xcode command-line tools)
- `codesign` (macOS)
- `zip` (macOS)
- `shasum` (macOS)
- `gh` (GitHub CLI, опционально для шага 3)

### Установка зависимостей

```bash
# GitHub CLI (опционально)
brew install gh

# Или скачайте Xcode Command Line Tools
xcode-select --install
```

---

## Отладка

### Ошибка: "Binary not found"

```bash
# Убедитесь, что Swift установлен
swift --version

# Попробуйте собрать вручную
swift build -c release
```

### Ошибка: "codesign failed"

Это нормально при ad-hoc подписи. Приложение все еще будет работать.

### Проверка bundlestructure

```bash
# Посмотрите содержимое bundle
ls -lR .build/release/HomebrewServicesManager.app/

# Проверьте Info.plist
cat .build/release/HomebrewServicesManager.app/Contents/Info.plist
```

---

## Автоматизация

Можно добавить алиасы в `.bash_profile` или `.zshrc`:

```bash
alias build-app="./build-app-bundle.sh"
alias publish-app="./PUBLISH.sh"
```

Тогда просто:

```bash
build-app 1.0
publish-app 1.0
```

---

## Примечания

- Скрипты локальные и не добавлены в git
- Каждый разработчик может использовать свою конфигурацию
- SHA256 хеш вычисляется автоматически
- Скрипты проверяют целостность на каждом шаге

