#!/usr/bin/env bash
set -euo pipefail

cat << "EOF"
╔═══════════════════════════════════════════════════════╗
║  Homebrew Services Manager - Первая сборка проекта   ║
╚═══════════════════════════════════════════════════════╝
EOF

echo ""
echo "Этот скрипт:"
echo "  1. Проверит структуру проекта"
echo "  2. Соберет CLI и App"
echo "  3. Запустит тесты"
echo "  4. Протестирует работу CLI"
echo ""

read -p "Продолжить? (y/n) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "Отменено."
    exit 0
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "ШАГ 1: Проверка структуры проекта"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Make scripts executable
chmod +x build.sh check-structure.sh

# Check structure
if [ -f "check-structure.sh" ]; then
    ./check-structure.sh
    if [ $? -ne 0 ]; then
        echo ""
        echo "❌ Структура проекта некорректна. Исправьте ошибки и попробуйте снова."
        exit 1
    fi
else
    echo "⚠️  check-structure.sh не найден, пропускаем проверку"
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "ШАГ 2: Сборка проекта"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

BUILD_APP=1 ./build.sh

if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Сборка не удалась. Проверьте вывод выше для деталей."
    exit 1
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "ШАГ 3: Запуск тестов"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

swift test

if [ $? -ne 0 ]; then
    echo ""
    echo "⚠️  Некоторые тесты не прошли, но сборка выполнена."
else
    echo ""
    echo "✅ Все тесты прошли успешно!"
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "ШАГ 4: Проверка CLI"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

if [ -f "dist/hsmanager-cli" ]; then
    echo "Запускаем CLI для получения списка сервисов..."
    echo ""
    ./dist/hsmanager-cli | head -n 20
    echo ""
    echo "✅ CLI работает!"
else
    echo "❌ CLI не найден в dist/hsmanager-cli"
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✨ ПЕРВАЯ СБОРКА ЗАВЕРШЕНА! ✨"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Что дальше:"
echo ""
echo "  📋 Запустить CLI:"
echo "     ./dist/hsmanager-cli"
echo ""
echo "  🖥️  Запустить GUI приложение:"
echo "     ./dist/HomebrewServicesManager"
echo ""
echo "  📖 Прочитать документацию:"
echo "     cat README_RU.md"
echo ""
echo "  🔧 Посмотреть все команды:"
echo "     make help"
echo ""
echo "  📝 Отчет об исправлениях:"
echo "     cat FIXES_REPORT.md"
echo ""
echo "Наслаждайтесь! 🎉"
echo ""
